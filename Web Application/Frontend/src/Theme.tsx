import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    main_bg: "#EFFBFF",
    main_blue: "#0B60B0",
    main_white: "#FFFFFF",
    main_black: "#000000",
    second_blue: "#40A2D8",
    main_beige: "#F0EDCF",
  },
});

export default theme;
